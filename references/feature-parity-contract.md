# Feature parity contract

The bundled
[authoritative as-built specification](query-lifecycle-two-level-walkthrough.spec.md) is
normative and wins every conflict. This document is supplementary implementation guidance only.
Generated HTML must implement all 93 tracked acceptance items in
[spec-compliance-manifest.json](spec-compliance-manifest.json). Canonical query text, raw plan
payloads, source snippets, and fixed example revisions remain excluded; query-specific values
and exact current-HEAD links are supplied by the validated runtime model.

## Normative reconciliation and parameterization

The canonical as-built behavior supersedes the earlier v2 rule that every substep has a runner.
S4.1, S4.4, S6.1, and S6.2 intentionally omit the runner, runner badge, lab, and placeholder.
All other canonical control, transition, omission, layout, and documented `[C]` behavior remains
normative.

Literal example data in the specification is parameterized where reuse or evidence safety
requires it:

- Azure DevOps links retain the canonical absolute, line-bounded shape but use the authorized
  workspace current HEAD and query-specific paths and lines, never the stale example revision.
- Query text, operator names, plan nodes, NodeIds, target scopes, schemas, method paths, runtime
  components, memory objects, and breakpoint text come from the validated query model.
- The canonical full raw plan example is never bundled. A raw-plan section appears only when
  authorized non-executing evidence permits it; otherwise the complete modeled tree remains
  visible and the limitation is explicit.
- Pass rails keep the canonical coordinate-specific capacities and interaction behavior, while
  pass names, predicates, transformations, and scheduled no-ops remain evidence-driven.
- Boundary node labels and byte offsets are evidence-driven. The canonical node-picker, three
  representations, 16-byte hex rows, 192-byte display limit, context-only recomputation, quiz,
  failure injection, and debugger-map behaviors are fixed.
- Execute timelines keep the canonical event and scenario counts. The first three execution
  substeps combine memory and lifetime into one scenario so three choices still cover failure,
  cancellation, memory, and lifetime; later substeps use four choices.

These substitutions change only canonical example values. They do not relax any interaction,
topology, safety, source-pinning, or evidence-label requirement.

User-assisted complete physical `QueryPlan` recovery is a pre-render evidence-acquisition
extension outside the canonical page parity surface. It changes neither the 93 acceptance items
nor the as-built specification. The renderer and publisher output report whether plan evidence
was automatic, user-supplied after recovery, or estimated only after an explicit recovery
outcome.

## Baseline gap

Static markup inventory was produced by `scripts/audit_html_features.py`. Dynamic content is
also generated by JavaScript, so these counts measure the shipped control surface rather than
the final number of data-driven cards.

| Surface | Canonical | Previous generated sample |
|---|---:|---:|
| Bytes | 427,809 | 88,448 |
| Lines | 6,741 | 482 |
| Buttons | 31 | 0 |
| Range controls | 4 | 0 |
| Collapsible `details` panels | 4 | 0 |
| Navigation regions | 7 | 2 |
| Sections | 34 | 2 |
| JavaScript functions | 71 | 6 |
| Stable element IDs | 167 | 18 |
| Remote runtime dependencies | 0 | 0 |

The previous renderer supplied a stage rail, a small action rail, one before/after view, a
physical tree, and a basic execution event. It lacked the canonical overview, substep,
traversal, artifact, experiment, deep-dive, boundary, execution-memory, and network-status
systems.

The machine-readable inventory enumerates five runner ecosystems, thirteen panel families,
all global and runner controls, all collapsible surfaces, all status/badge families, all
experiment/scenario families, every source-link-bearing surface, and every tree/data
visualization family. Its `previous_sample_gaps` list is the enforced semantic delta from the
deficient output; byte and tag counts are diagnostic baselines, not parity substitutes.

## Required global surfaces

Every generated page must expose the semantic feature IDs listed in the JSON inventory:

- ten-stage navigation;
- a collapsible stage overview with input, output, owner, exclusions, and handoff;
- query-specific substep cards with behavior, change, and runner-count badges;
- previous, next, and range navigation that updates content in place;
- detailed what/why/debug/next/source/method-path content;
- linked traversal nodes with current, movement, return, and next state plus
  previous/play/range/next controls;
- collapsible before/after artifacts with linked operators;
- a stateful outbound-source beacon showing state, route, and purpose;
- keyboard navigation, visible focus, responsive layouts, reduced-motion behavior, and print
  output.

## Canonical runner topology

The ten stages contain exactly `4, 5, 4, 4, 5, 4, 5, 5, 3, 6` substeps. Exactly four
substeps omit the runner entirely: S4.1, S4.4, S6.1, and S6.2. They show no runner badge,
no lab, and no placeholder. The other 41 substeps use the position-specific runner family and
interaction counts in `scripts/canonical_spec.py`. A present runner must have:

- reset, previous, next, apply/execute, range, and action-rail controls;
- two or more distinct visible snapshots;
- before/after state, progress, result, and exact source;
- one or more interactive experiments with option-specific results;
- no inert controls.

The validator rejects omitted required runners, runners at the four omission coordinates,
mismatched position-specific runner types/counts, duplicate snapshots, unexplained no-ops,
pass outcome contradictions, and incomplete source grounding.

## Specialized systems

### Compiler

Syntax, Semantic, and Relop substeps use compiler runners. They show action states, progress,
exact source, unique experiments, and semantic or CSL-to-Relop mapping where applicable.

### Pass pipeline

Preparation and all optimization phases use pass runners. Only passes applicable to this query
may appear. Each pass includes traversal, predicate, applicability, optimization target,
cumulative colored diff, source, rule, and result. A scheduled pass with an unchanged tree is
explicitly `SCHEDULED_NO_OP`, never a claimed transformation.

### Physical plan

S8.1 through S8.4 have physical runners and input-contract collapses. S8.5 switches to a pass
runner and is the only location of the physical-plan deep dive. The deep dive carries
the complete evidenced plan, linked operators, logical-to-physical mapping, schemas, node IDs,
key indexes, execution/Rust eligibility, target scope, remote metadata, and only those raw
sections allowed by the evidence policy.

### Serialize/native boundary

Every boundary substep has a runner with managed, JSON, UTF-8, and native lanes; object/JSON/
byte views; evidenced node ranges; context comparisons; failure injection; and a debugger map.

### Execute

Every query-present runtime operator or boundary has a runner. It includes a user-driven
timeline, language lanes, what/why/stack/heap effects, cumulative conceptual stack (top first),
applicable lifetime zones and states, and active/waiting/not-created components below stack
and heap. It also exposes failure, cancellation, memory, and lifetime scenarios, pull/data
direction, current ownership, and next breakpoint.

## Evidence and safety

All action, traversal, artifact, operator, frame, zone, component, scenario, and experiment
links are exact-line Azure DevOps HTTPS URLs pinned to the authorized workspace HEAD. The
renderer does not execute the supplied query. Unsupported claims remain visibly `ESTIMATED`.
The page is a self-contained offline artifact and model text is embedded with script-safe JSON
escaping and rendered through `textContent`.
